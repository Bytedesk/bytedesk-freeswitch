# FreeSWITCH Docker 环境变量配置示例
# 复制此文件为 .env 并修改相应的值
# cp .env.example .env

# ⚠️  安全警告：
# 1. 必须修改所有默认密码
# 2. 不要将 .env 文件提交到版本控制系统
# 3. 确保 .env 文件权限为 600 (chmod 600 .env)
# 4. 详细安全配置请参考：docker/SECURITY.md

# ==========================================
# 🔐 安全配置（必填 - 生产环境必须修改）
# ==========================================

# ESL 管理密码（必须设置强密码）
# 警告：此密码可以完全控制 FreeSWITCH 系统
# 建议：至少 16 位，包含大小写字母、数字和特殊字符
# 示例强密码：MyStr0ng#ESL!Pass2024
FREESWITCH_ESL_PASSWORD=CHANGE_THIS_ESL_PASSWORD

# SIP 用户默认密码（强烈建议修改）
# 警告：所有 SIP 用户（1000-1019）默认使用此密码
# 默认值为 1234，生产环境必须修改！
# 建议：至少 12 位强密码
# 示例强密码：MyStr0ng#SIP!Pass2024
FREESWITCH_DEFAULT_PASSWORD=CHANGE_THIS_SIP_PASSWORD

# ==========================================
# 🌐 网络配置
# ==========================================

# SIP 域名或 IP 地址
# 示例：sip.yourdomain.com 或 203.0.113.10
FREESWITCH_DOMAIN=localhost

# 外部 IP 地址（NAT 穿透必需）
# 用于公网访问时的 SIP 和 RTP 地址
# 获取方法：curl ifconfig.me 或 curl ipinfo.io/ip
# 示例：203.0.113.10
# FREESWITCH_EXTERNAL_IP=

# RTP 端口范围
FREESWITCH_RTP_START=16384
FREESWITCH_RTP_END=32768

# ==========================================
# 数据库配置（可选）
# ==========================================

# 是否启用数据库
# FREESWITCH_DB_ENABLED=false

# MySQL/MariaDB 配置
# FREESWITCH_DB_HOST=mysql
# FREESWITCH_DB_NAME=freeswitch
# FREESWITCH_DB_USER=freeswitch
# FREESWITCH_DB_PASSWORD=secure_password
# FREESWITCH_DB_PORT=3306

# ==========================================
# 系统配置
# ==========================================

# 时区设置
TZ=Asia/Shanghai

# ==========================================
# Baidu MRCP（容器内集成）
# ==========================================

# 启用/禁用 Baidu MRCP Server（1=启用，0=禁用）
BAIDU_MRCP_ENABLE=1

# MRCP 使用的 SIP 端口，避免与 FreeSWITCH 5060 冲突（无需对外暴露）
BAIDU_MRCP_SIP_PORT=5070

# 百度认证信息（请替换为真实值）
BAIDU_APPID=CHANGE_THIS_APPID
BAIDU_API_KEY=CHANGE_THIS_API_KEY
BAIDU_SECRET_KEY=CHANGE_THIS_SECRET

# 是否保存音频（1=保存，0=不保存）
BAIDU_MRCP_SAVE_AUDIO=1

# ==========================================
# 高级配置（通常不需要修改）
# ==========================================

# FreeSWITCH 安装路径
# FREESWITCH_PREFIX=/usr/local/freeswitch

# 日志级别（DEBUG, INFO, NOTICE, WARNING, ERROR, CRIT, ALERT）
# FREESWITCH_LOG_LEVEL=INFO

# 最大会话数
# FREESWITCH_MAX_SESSIONS=1000

# ==========================================
# WebRTC 配置
# ==========================================

# WebRTC 启用
# FREESWITCH_WEBRTC_ENABLED=true

# STUN 服务器
# FREESWITCH_STUN_SERVER=stun.freeswitch.org

# ==========================================
# 安全配置
# ==========================================

# 是否允许匿名 SIP 注册
# FREESWITCH_ALLOW_ANONYMOUS=false

# ACL 白名单（逗号分隔的 IP 地址）
# FREESWITCH_ACL_WHITELIST=127.0.0.1,192.168.0.0/16

# ==========================================
# 性能配置
# ==========================================

# CPU 核心数限制
# FREESWITCH_CPU_CORES=2

# 内存限制（MB）
# FREESWITCH_MEMORY_LIMIT=2048

# ==========================================
# 开发/调试配置
# ==========================================

# 启用调试模式
# FREESWITCH_DEBUG=false

# 详细日志
# FREESWITCH_VERBOSE=false

# ==========================================
# 备注说明
# ==========================================

# 1. 生产环境必须修改 FREESWITCH_ESL_PASSWORD
# 2. 云服务器部署必须设置 FREESWITCH_EXTERNAL_IP
# 3. 数据库配置是可选的，用于持久化存储
# 4. RTP 端口范围根据并发通话数调整
# 5. 时区设置影响日志时间戳
# 6. WebRTC 配置用于 Web 端通话
# 7. 安全配置建议在生产环境启用
